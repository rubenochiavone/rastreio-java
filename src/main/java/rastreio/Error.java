package rastreio;

/**
 * Tracking object error results.
 */
public enum Error {
  /**
   * No error occurred during object tracking.
   */
  NO_ERROR,
  /**
   * Unable to track object with given object code.
   */
  OBJECT_NOT_FOUND
}
